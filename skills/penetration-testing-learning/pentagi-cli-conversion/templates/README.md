# Templates

This directory is reserved for reusable PentAGI-to-CLI conversion templates.

Current workflow relies on the references in this skill; add concrete templates here only when they are reusable across multiple conversions.
